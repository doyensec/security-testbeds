version: "3.8"

services:
  gateway-vuln:
    build:
      context: ./gateway
      args:
        SCG_VERSION: 2021.0.0
    ports:
      - "8081:8080"

  gateway-safe:
    build:
      context: ./gateway
      args:
        SCG_VERSION: 2021.0.1
    ports:
      - "8082:8080"